import { ActiviteitResEntity } from './activiteit-res-entity';

describe('ActiviteitResEntity', () => {
  it('should be defined', () => {
    expect(new ActiviteitResEntity()).toBeDefined();
  });
});
