import { ActiviteitPlanningEntity } from './activiteit-planning-entity';

describe('ActiviteitPlanningEntity', () => {
  it('should be defined', () => {
    expect(new ActiviteitPlanningEntity()).toBeDefined();
  });
});
