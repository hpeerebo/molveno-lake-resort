import { CreateIngredientDto } from './create-ingredient-dto';

describe('CreateIngredientDto', () => {
  it('should be defined', () => {
    expect(new CreateIngredientDto()).toBeDefined();
  });
});
