import { ActiviteitPlanningPipe } from './activiteit-planning.pipe';

describe('ActiviteitPlanningPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiviteitPlanningPipe();
    expect(pipe).toBeTruthy();
  });
});
