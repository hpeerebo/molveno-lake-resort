import { ActiviteitenPipe } from './activiteiten.pipe';

describe('ActiviteitenPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiviteitenPipe();
    expect(pipe).toBeTruthy();
  });
});
