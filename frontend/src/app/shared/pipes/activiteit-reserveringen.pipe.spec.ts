import { ActiviteitReserveringenPipe } from './activiteit-reserveringen.pipe';

describe('ActiviteitReserveringenPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiviteitReserveringenPipe();
    expect(pipe).toBeTruthy();
  });
});
